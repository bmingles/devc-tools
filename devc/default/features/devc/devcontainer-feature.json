{
  "id": "devc",
  "version": "1.0.0",
  "name": "devc baseline",
  "description": "devc's baseline runtime setup (postCreate): ownership of the isolated ~/.claude volume, the per-workspace ~/.claude.json symlink, and nvm install from the project's .nvmrc. Build-time setup (Claude CLI, shell additions) is baked by the bundled Dockerfile.",
  "options": {},
  "installsAfter": [
    "ghcr.io/devcontainers/features/node"
  ],
  "postCreateCommand": "/usr/local/share/devc/post-create.sh"
}
