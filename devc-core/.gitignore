# npm build output (`npm run build`): dist/mod.js + .d.ts files, plus default/ copied beside it
/dist/

# npm dependencies (@devcontainers/cli, jsonc-parser, esbuild, typescript)
/node_modules/

# `npm pack` output — built on demand to test a consumer before publishing
*.tgz
