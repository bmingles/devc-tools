{
  "id": "devc-bridge",
  "version": "0.1.0",
  "name": "devc-bridge (container client)",
  "description": "Lets a devcontainer invoke allowlisted commands on the host via devc-bridge. Mounts the host bridge's token and Linux client read-only and puts `devc-bridge` on PATH. Requires the host bridge to be installed first.",
  "documentationURL": "https://github.com/bmingles/devc-tools/tree/main/features/devc-bridge",
  "mounts": [
    "type=bind,source=${localEnv:HOME}/.config/devc-bridge/run,target=/run/devc-bridge,consistency=cached,readonly",
    "type=bind,source=${localEnv:HOME}/.config/devc-bridge/client,target=/usr/local/share/devc-bridge/client,consistency=cached,readonly"
  ]
}
